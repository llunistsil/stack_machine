in_source_code: |
  /in a_hi;
  /in a_lo;
  /in b_hi;
  /in b_lo;
  # Сложение 64-битных чисел: (a_hi, a_lo) + (b_hi, b_lo)
  sum_lo = a_lo + b_lo;
  carry = 0;
  if sum_lo < a_lo : carry = 1; ;
  sum_hi = a_hi + b_hi + carry;
  > sum_hi;
  > sum_lo;
in_input_data: "1 4294967295 2 1"
in_output_format: "text"
in_log_level: "trace"
out_compiled: ""
out_log: ""
out_output: "3 0"