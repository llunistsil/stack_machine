in_source_code: |
  /in a;
  /in b;
  /in c;
  if a > b : t = a; a = b; b = t; ;
  if b > c : t = b; b = c; c = t; ;
  if a > b : t = a; a = b; b = t; ;
  > a;
  > b;
  > c;
in_input_data: "3 1 2"
in_output_format: "text"
in_log_level: "trace"
out_compiled: ""
out_log: ""
out_output: "1 2 3"